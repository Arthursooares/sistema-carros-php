services:
  php:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: bd
    volumes:
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql